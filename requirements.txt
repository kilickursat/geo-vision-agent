streamlit
smolagents[all]
transformers
huggingface-hub
requests
pandas
numpy
plotly
python-dotenv
statsmodels
# Use alternative packages for PDF processing that have fewer dependencies
PyPDF2
Pillow
# Add setuptools explicitly to handle distutils dependency
setuptools

# SmolDocling dependencies
torch
docling_core

# Optional: Only include if absolutely needed
pdf2image
python-magic
